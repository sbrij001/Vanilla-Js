// DOM NOTES

// the dom is bascially an upside down tree
// the roots are at the top of the dome
// and the leaves are the deeply nested HTML elements

// every tree contains subtrees
// these subtrees are independent from their parent trees
